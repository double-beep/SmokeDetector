# Before adding new users here, make sure they are in users.yml
stackexchange.com:
  109232:
    commands: true

    msg_types:
      - debug
      - metasmoke
      - all-sites
      - experimental-all-sites
      - no-offensive-mask

    privileges:
      - 17078244
